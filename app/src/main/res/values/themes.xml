<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="FoodDeliveryApplication" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="Theme.FoodDeliveryApplication.NoActionbar" parent="FoodDeliveryApplication">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="roundedImageView" parent="">
        <item name="cornerSize">@dimen/space_10</item>
    </style>

    <style name="BottomNavigation">
        <item name="itemIconTint">@color/bottom_navigation_menu</item>
        <item name="itemTextColor">@color/bottom_navigation_menu_text</item>
    </style>

    <style name="Category">
        <item name="itemIconTint">@color/bottom_navigation_menu</item>
        <item name="itemTextColor">@color/bottom_navigation_menu_text</item>
    </style>
</resources>